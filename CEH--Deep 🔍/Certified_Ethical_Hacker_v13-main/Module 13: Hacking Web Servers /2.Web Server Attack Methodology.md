# 🧠 Web Server Attack Methodology
A web server attack typically involves preplanned activities called an attack methodology that an attacker follows to reach the goal of breaching the target web server’s security. Attackers hack a web server in multiple stages. At each stage, the attacker attempts to gather information about loopholes and to gain unauthorized access to the web server. The following are the various stages of the attack methodology for web servers. 

- **Information Gathering** <br>
  Every attacker tries to collect as much information as possible about the target web server. The attacker gathers the information and then analyzes it to find lapses in the current security mechanisms of the web server.

- **Web Server Footprinting** <br>
  The purpose of footprinting is to gather information about the security aspects of a web server with the help of tools or footprinting techniques. Through footprinting, attackers can determine the web server’s remote access capabilities, its ports and services, and other aspects of its security.

- **Website Mirroring** <br>
  Website mirroring is a method of copying a website and its content onto another server for offline browsing. With a mirrored website, an attacker can view the detailed structure of the website.

- **Vulnerability Scanning** <br>
  Vulnerability scanning is a method of finding the vulnerabilities and misconfigurations of a web server. Attackers scan for vulnerabilities with the help of automated tools known as vulnerability scanners.
  
- **Session Hijacking** <br>
  Attackers can perform session hijacking after identifying the current session of the client. The attacker takes complete control over the user session through session hijacking.

- **Web Server Passwords Hacking** <br>
  Attackers use password-cracking methods such as brute-force attacks, hybrid attacks, and dictionary attacks to crack the web server’s password.

---
## Information Gathering 
Information gathering is the first step in hacking a web server. In this phase, the attacker collects details about the target, like domain name, IP address, and server info, using tools like **Whois** or **who.is**. This helps them understand the server’s setup and find possible weaknesses to attack later.

- **who.is** 🔗Source: [https://who.is] <br>
  who.is is designed perform a variety of whois lookup functions. It lets the user perform a domain whois search, whois IP lookup, and whois database search for relevant information on domain registration and availability.

  <img width="288" height="175" alt="image" src="https://github.com/user-attachments/assets/39a144cc-ada4-478e-befc-ebc38bce98e6" />

  <img width="417" height="467" alt="image" src="https://github.com/user-attachments/assets/7c78b610-a08a-4e9b-b2c7-289afb241d7f" />

**The following are some additional information-gathering tools:**
- **Whois Lookup** [https://whois.domaintools.com]
- **Whois** [https://www.whois.com]
- **Domain Dossier** [https://centralops.net]
- **Subdomain Finder** [https://pentest-tools.com]

### Information Gathering from Robots.txt File 
The `robots.txt` file tells search engines which parts of a website to ignore. But if it's not written carefully, it can accidentally reveal hidden or sensitive areas of the site. Attackers can easily view this file by typing `site.com/robots.txt` in a browser or downloading it with tools like Wget. From there, they may find secret files, links, or folders and use that information to gather data like passwords or private content.

<img width="239" height="393" alt="image" src="https://github.com/user-attachments/assets/02e2ed94-1e99-4607-83be-59f8d6a26714" />

---
## Web Server Footprinting/Banner Grabbing
Web server footprinting or banner grabbing is a technique used to collect information about a target web server, such as its software version, operating system, server name, and running services. Attackers use tools like Telnet, httprecon, Uniscan, and Netcraft to gather this data. This information helps them find weaknesses in the server that they can exploit.

### Web Server Footprinting Tools 
- **Netcat** 🔗Source: [https://netcat.sourceforge.net] <br>
  Netcat is a networking utility that reads and writes data across network connections by using the TCP/IP protocol. It is a reliable “back-end” tool used directly or driven by other programs and scripts. It is also a network debugging and exploration tool.

  The following are the commands used to perform banner grabbing for **www.moviescope.com** as an example to gather information such as server type and version.
  ```bash
  nc –vv www. moviescope.com 80  # press [Enter]
  GET / HTTP/1.0   # press [Enter] twice
  ```
  <img width="441" height="242" alt="image" src="https://github.com/user-attachments/assets/3e268ee5-590a-42d1-9b26-b072f0656ddd" />

- **Telnet** 🔗Source: [https://learn.microsoft.com] <br>
  Telnet is a network protocol that lets users remotely log in to another computer over the Internet or a local network. However, it's not secure because it sends data without encryption and doesn't have proper authentication. Attackers can use Telnet for **banner grabbing**, which helps them gather information about the system or web server, such as software type and version, making it easier to plan attacks.

  For instance, the following procedure is utilized to enumerate a host running on HTTP (TCP 80).
    - Request Telnet to connect to a host on a specific port with the command `telnet www.moviescope.com 80` and press **Enter**. A blank screen appears.
    - Type `GET / HTTP/1.0` and press **Enter twice**.
      
  The HTTP server responds with the information shown in the screenshot.
  
  <img width="443" height="277" alt="image" src="https://github.com/user-attachments/assets/c2938662-a2c1-42af-b686-b31ecbd75b00" />

- **httprecon** 🔗Source: [https://www.computec.ch] <br>
	**httprecon** is a web server fingerprinting tool used to gather detailed information about a target web server. It sends different types of HTTP requests—both normal and unusual—to analyze how the server responds. This includes testing with valid and invalid methods, long requests, and fake protocols. Based on these responses, httprecon helps identify the web server type and its behavior, which is useful for security testing and vulnerability assessment.

  httprecon performs the following header analysis test cases on the target web server:
  - A legitimate GET request for an existing resource
  - An exceedingly long GET request (a Uniform Resource Identifier (URI) of >1024 bytes)
  - A common GET request for a non-existing resource
  - A common HEAD request for an existing resource
  - Enumeration with OPTIONS, which is allowed
  - The HTTP method DELETE, which is usually not permitted
  - The HTTP method TEST, which is not defined
  - The protocol version HTTP/9.8, which does not exist
  - A GET request including attack patterns (e.g., : ../ and %%)

 	<img width="650" height="458" alt="image" src="https://github.com/user-attachments/assets/2f51018c-1977-4ce5-b008-82ba2ed0e64f" />

- **Uniscan** 🔗Source: [https://sourceforge.net]
  Uniscan is a powerful tool used to gather information about web servers. It can run basic commands like ping, traceroute, and nslookup, and also perform deep scans to check for weaknesses. It scans websites, runs Google and Bing searches for specific IPs, and creates a full report with the results.

	<img width="608" height="425" alt="image" src="https://github.com/user-attachments/assets/3605ecbc-99a5-4482-9b80-720ff067831b" />

**The following are some additional footprinting tools:** 
- **Netcraft** [https://www.netcraft.com]
- **ID Serve** [https://www.grc.com]
- **Nmap** [https://nmap.org]
- **Ghost Eye** [https://github.com/BullsEye0/ghost_eye]
- **Skipfish** [https://code.google.com]

---
## Web Server Footprinting with AI 
Attackers can leverage AI-powered technologies to enhance and automate hacking. With the aid of AI, attackers can effortlessly perform web server footprinting on the target servers. For example, An attacker can use ChatGPT to perform this task by using an appropriate prompt such as:
- **“Perform webserver footprinting on target IP 10.10.1.22”**

<img width="589" height="382" alt="image" src="https://github.com/user-attachments/assets/4ee6c1be-fa34-4b86-8e3c-e0a69500861c" />

<img width="590" height="354" alt="image" src="https://github.com/user-attachments/assets/67ad8ac2-f89d-4540-b146-42267ded0107" />

The output of the prompt results in the following command. `nmap -SV 10.10.1.22 && whatweb 10.10.1.22 && nikto -h 10.10.1.22`

---
## Web Server Footprinting using Netcat with AI 
Attackers can leverage AI-powered technologies to enhance and automate hacking. With the aid of AI, attackers can effortlessly perform footprinting on the target Web server with the help of the Netcat tool. For example, An attacker can use ChatGPT to perform this task by using an appropriate prompt such as: 
- **“Perform webserver footprinting on target IP 10.10.1.22 with netcat”**

<img width="549" height="318" alt="image" src="https://github.com/user-attachments/assets/8ef47a5a-9669-48a3-8979-76f975d3b496" />

The command you provided is using `nc` (netcat) to establish a connection to the target IP address "10.10.1.22" on port 80, and then sending an HTTP request using the `HEAD` method. 
```bash
# Open a TCP connection to IP 10.10.1.22 on port 80 (HTTP)
nc -v 10.10.1.22 80 <<EOF
# Send an HTTP HEAD request (fetch headers only, no body)
HEAD / HTTP/1.1
# Specify the Host header (required in HTTP/1.1)
Host: 10.10.1.22
EOF
```
---
## IIS Information Gathering using Shodan 
IIS Information Gathering using Shodan involves collecting details about IIS servers to find security weaknesses. Attackers use tools like Shodan to identify the IIS version, open ports, services, and configurations. If the server is outdated or misconfigured, attackers can use known exploits to target it. This information helps them plan more focused and effective attacks.

Shodan search filters that can be used by attackers to gather information on IIS servers are as follows:
- Use the following filter to search for any IIS server and to provide a list of instances running IIS: `http.title:"IIS"`
- Use the following filter to identify IIS servers with SSL certificates issued to "Company_name" that can assist in finding servers associated with a specific organization: `Ssl:"Company Inc." http.title:"IIS"`

  <img width="529" height="490" alt="image" src="https://github.com/user-attachments/assets/5802d139-7f9b-4496-826c-2065ccd9ea02" />
  
- Use the following filter to locate IIS servers with SSL certificates where the common name (CN) is "company.in": `Ssl.cert.subject.CN:"company.in" http.title:"IIS"` <br> This can assist in identifying servers associated with a specific domain or subdomain.
- Use the following filter to identify IIS servers in the US that can aid in geographically targeted attacks: `http.title:"IIS Windows Server" country:"US"`

  <img width="582" height="508" alt="image" src="https://github.com/user-attachments/assets/968e1ce4-f699-4ebe-862e-3eb9dbcb1f97" />

- Use the following filter to locate IIS7 servers running on port 80: `http.title:"IIS7" port:80`

  <img width="592" height="525" alt="image" src="https://github.com/user-attachments/assets/166933f7-28d6-420c-afe2-662d0b6d55f8" />

- Use the following filter to search for IIS7 servers within a specific IP range to assist in network-specific information gathering: `http.title:"IIS7" net:"<IP_address>/24"`

Additional Shodan search filters that can be used by attackers to gather information on IIS servers are as follows:
- `http.title:"IIS7"` – Identifies IIS servers running version 7, useful for finding specific version vulnerabilities.
- `http.title:"IIS Windows Server"` - Targets IIS servers on Windows, helpful for Windows-specific exploitation.
- `http.title:"Internet Information Services"` - Broad search for any IIS servers, useful for general information gathering.

---
## Abusing Apache mod_userdir to Enumerate User Accounts
Attackers can misuse Apache’s `mod_userdir` feature to find valid usernames on a web server. This module lets users access their personal web directories using URLs like `http://site.com/~username/`. By using tools like Nmap to test different names, attackers can discover which usernames exist. This information can help them launch further attacks like brute-force or phishing.

### Enumerating User Accounts from the Apache Server 
- **Perform Initial Scan to Enumerate Valid Users** - Run the following command to enumerate valid users from the target web server with mod_userdir enabled: <br>
	```bash
	nmap -p80 --script http-userdir-enum <target>
 	```
	This command scans the target on port 80 using the `‘http-userdir-enum’` script. If `‘mod_userdir’` is enabled, the script uses the default word list `‘usernames.lst’` located at `‘/nselib/data/’` and lists any found usernames.

- **Perform Customized Scan** - A customized list of potential usernames can be used to improve the accuracy and effectiveness of the attacks. Run the following command with the custom word list ‘<Wordlist>.txt’:
	```bash
 	nmap -p80 --script http-userdir-enum userdir.users=<Wordlist>.txt <target>
 	```
	This command takes the ‘.txt’ file as a source for the usernames and tests against the target.

- **Bypass Detection with Custom User Agent** - Some security systems may detect and block requests from Nmap because of its default user-agent string. To bypass such detection, a custom user agent should be specified. Run the following command with the specified HTTP user-agent string to avoid detection:
	```bash
 	nmap -p80 --script http-brute --script-args http.useragent="<User_Agent>" <target>
 	```
	This command changes the HTTP user-agent string used by Nmap to a specified string, making the traffic appear to originate from a standard web browser rather than a scanning tool.

---
## Enumerating Web Server Information Using Nmap 
Nmap, along with the Nmap Scripting Engine (NSE), can extract a large amount of valuable information from the target web server. In addition to Nmap commands, NSE provides scripts that reveal various types of useful information about the target server to an attacker. <br>
An attacker uses the following Nmap commands and NSE scripts to extract information. 
```bash
nmap --script hostmap-bfk <host>			# → Discover virtual domains with hostmap
nmap --script http-trace -p80 localhost			# → Detect a vulnerable server that uses the TRACE method
nmap --script http-google-email <host>			# → Harvest email accounts with http-google-email
nmap -p80 --script http-userdir-enum localhost		# → Enumerate users with http-userdir-enum 
nmap -p80 --script http-trace <host>			# → Detect HTTP TRACE
nmap --script=http-waf-fingerprint -p80,443 <host>	# → Fingerprint a WAF 
nmap --script http-enum -p80 <host>			# → Enumerate common web applications
nmap -p80 --script http-robots.txt <host>		# → Obtain robots.txt

# → Check if the web server is protected by a web application firewall (WAF) or IPS:
nmap -p80 --script http-waf-detect --script-args=”http-waf-detect.uri=/testphp.vulnweb.com/artists.php,http-waf-detect.detectBodyChanges” www.modsecurity.org
```
The following are some additional Nmap commands used to extract web server information: 
```bash
# Step 1: Scan target for open ports, service versions, and operating system
nmap -sV -O -p <TargetIP>
# → -sV: Detects service versions
# → -O: Attempts OS detection
# → -p: Mistaken here; it should be followed by port numbers (e.g., -p 80), not an IP

# Step 2: Enumerate common web application directories on the target
nmap -sV --script http-enum <TargetIP>
# → Runs the 'http-enum' NSE script to find web directories and apps (e.g., admin, login)

# Step 3: Scan for FrontPage login pages on port 80
nmap <TargetIP> -p 80 --script=http-frontpage-login
# → Detects Microsoft FrontPage login forms if present on port 80

# Step 4: Attempt to retrieve password files from web servers
nmap --script http-passwd --script-args http-passwd.root=/ <TargetIP>
# → Tries to access common password files (e.g., /etc/passwd) via misconfigured web servers
# → 'http-passwd.root=/' sets the root directory for the script to scan from
```

## Finding Default Credentials of Web Server 
Attackers often look for web servers with admin panels that still use default usernames and passwords. These panels are usually easy to find and access if not properly secured. Attackers attempt to identify the running application interface of the target web server by performing port scanning. Once the running administrative interface is identified, the attacker uses the following techniques to identify the default login credentials: 
- Consult the administrative interface documentation and identify the default passwords 
- Use Metasploit’s built-in database to scan the server 
- Use online resources such as **cirt.net** [https://cirt.net/passwords] and **FortyPoundHead.com** [https://www.fortypoundhead.com] to identify the default passwords
- Attempt password-guessing and brute-forcing attacks
These default credentials can grant access to the administrative interface, compromising the web server and allowing the attacker to exploit the main web application.

The following are some additional websites for finding the default passwords of web server administrative interfaces: 
- **cirt.net** [https://cirt.net/passwords]
- [https://www.fortypoundhead.com]
- [https://www.defaultpassword.com]
- [https://default-password.info]
- [https://www.routerpasswords.com]

---
## Finding Default Content of Web Server 
Most servers of web applications have default contents and functionalities that allow attackers to launch attacks. The following are some common default contents and functionalities that an attacker attempts to identify in web servers. 

- **Administrators debug and test functionality** <br>
	Admin tools used for testing and debugging can reveal sensitive server details. Hackers target these tools to understand how the server works and find weaknesses.

- **Sample functionality to demonstrate common tasks** <br>
	Web servers often have demo scripts or sample pages. If not secured, attackers can exploit these to gain access or perform harmful actions.

- **Publicly accessible powerful functions** <br>
	Some web features meant only for admins might be exposed to the public. Hackers use tools like Metasploit to find and exploit these to upload malware or take control of the server.

- **Server installation manuals** <br>
	Attackers look for installation or setup guides stored on the server. These manuals can reveal default settings or configurations, making it easier to plan attacks. Tools like **Nikto2** [https://cirt.net] help find such content.

---
## Directory Brute Forcing 
When a web server receives a request for a directory, rather than a file, the web server responds to the request in the following ways.
- **Return Default Resource within the directory** <br>
	The server may return a default resource within the directory, such as index.html.
- **Return Error** <br>
	The server may return an error, such as the HTTP status code 403, indicating that the request is not permitted.
- **Return listing of directory content** <br>
	The server may return a listing showing the contents of the directory.  A sample directory listing is shown in the screenshot.

<img width="269" height="359" alt="image" src="https://github.com/user-attachments/assets/21998bc1-7bce-46a1-978c-fc6909f3ad03" />

Directory listings may seem harmless but can expose web applications to risks if not properly secured. Weak access controls or accidental exposure of the server's root directory can let attackers view or access sensitive files. Hackers use tools like **Dirhunt** [https://github.com/Nekmo/dirhunt] and **Sitechecker** [https://sitechecker.pro/] to find and exploit these open directories on web servers.

----
## Directory Brute Forcing with AI 
An attacker can use ChatGPT to perform this task by using an appropriate prompt such as:
- **“Perform a directory traversal on target url https://certifiedhacker.com”**

  <img width="461" height="296" alt="image" src="https://github.com/user-attachments/assets/a0c38bda-5d29-43c2-8064-d5b57ff87433" />

---
## Vulnerability Scanning 
Vulnerability scanning is the process of checking a web server or network for security weaknesses or misconfigurations. Attackers or security professionals use tools like **Acunetix** to scan for open ports, running services, and known vulnerabilities. They may also sniff network traffic to learn more about active devices and software. This helps identify weak spots that could be targeted in an attack.

- **Acunetix Web Vulnerability Scanner** 🔗Source: [https://www.acunetix.com] <br>
	Acunetix Web Vulnerability Scanner (WVS) is a tool used to find security flaws in websites. It scans for common issues like SQL injection and XSS, even in login-protected or CAPTCHA-enabled pages. It supports modern web technologies like HTML5, SOAP, and AJAX. Acunetix also checks the server, detects the web app's language and server type, and can generate detailed security reports to help meet compliance standards.

<img width="618" height="413" alt="image" src="https://github.com/user-attachments/assets/97187473-2f09-47a6-a659-61bf49e2914e" />

The following are some additional vulnerability scanning tools:
- **OpenText Fortify WebInspect** [https://www.opentext.com]
- **Tenable.io** [https://www.tenable.com]
- **ImmuniWeb** [https://www.immuniweb.com]
- **Invicti** [https://www.invicti.com]

---
## Nginx Vulnerability Scanning using Nginxpwner 
🔗Source: [https://github.com/stark0de/nginxpwner]

**NginxPwner** is a Python tool used to scan Nginx web servers for security issues and misconfigurations. It checks for problems like weak HTTP methods, wrong file permissions, outdated versions, and unsafe default settings. This helps attackers find weak spots in the server to plan targeted attacks.

### Steps to Scan the Target Nginx Web Server for Vulnerabilities 
```bash
nano /tmp/pathlist 	# Create a temporary file to store a list of potential URL paths acquired via crawling or other methods.
python3 nginxpwner.py <target_URL> /tmp/<pathlist> 
```
<img width="489" height="240" alt="image" src="https://github.com/user-attachments/assets/813cd0f7-18fe-417c-9739-ddd11706833c" />

Analyze the output provided by NginxPwner to identify potential vulnerabilities and misconfigurations in the target Nginx server.

---
## Finding Exploitable Vulnerabilities 
Finding exploitable vulnerabilities means looking for weaknesses or coding mistakes in software that attackers can use to break into systems. These flaws can exist in applications, services, the operating system, or even the kernel. Hackers search public exploit websites like Packet Storm [https://packetstormsecurity.com] or Exploit Database [https://www.exploit-db.com) ] to find known vulnerabilities related to the target’s OS or software. By using this info, they can run malicious code, gain higher access rights, or bypass security protections to take control of the system.

---
## Finding Exploitable Vulnerabilities with AI 
An attacker can use ChatGPT to perform this task by using an appropriate prompt such as: 
- **“Identify OS and services running on the target 10.10.1.19 and then install and launch the searchsploit to find out the possible exploits associated with OS and services identified”**

<img width="541" height="290" alt="image" src="https://github.com/user-attachments/assets/35ad1b9f-8223-4770-ade3-9a089abb10fe" />

The output of the prompt results in the following command:
```bash
sudo apt-get update && sudo apt-get install nmap -y && nmap -SV-0 10.10.1.19 -oX nmap_scan.xml && sudo apt-get install exploitdb -y && searchsploit-nmap nmap_scan.xml
```
Explenation
```bash
# Step 1: Update package list and install Nmap
sudo apt-get update && sudo apt-get install nmap -y
# → Makes sure your system is updated and installs Nmap for network scanning

# Step 2: Scan the target IP with service detection, and save output in XML
nmap -SV-0 10.10.1.19 -oX nmap_scan.xml
# → Scans target 10.10.1.19 for running services (SV-0 is likely a typo, should be -sV -O)
# → Results are saved to an XML file for later use

# Step 3: Install Exploit Database tools
sudo apt-get install exploitdb -y
# → Installs tools like `searchsploit` to find public exploits

# Step 4: Search for known exploits related to scanned services
searchsploit-nmap nmap_scan.xml
# → Matches the scanned services with known vulnerabilities/exploits from ExploitDB
```
This command automates the process of identifying potential vulnerabilities and searching for associated exploits on the target IP address 10.10.1.19, enabling attackers to exploit any discovered vulnerabilities for malicious purposes.

---
## Session Hijacking 
**Session hijacking** is when an attacker steals a valid session ID to take control of a user’s active session with a web server. This lets them access sensitive data without logging in. Attackers use methods like session prediction, replay, fixation, sidejacking, or XSS to grab session cookies. Tools like Burp Suite, JHijack, and Ettercap help automate these attacks.

- **Burp Suite** 🔗Source: [https://portswigger.net] <br>
	Burp Suite is a web security testing tool that can hijack session IDs in established sessions. The Sequencer tool in Burp Suite tests the randomness of session tokens. With this tool, an attacker can predict the next possible session ID token and use that to take over a valid session.

<img width="591" height="407" alt="image" src="https://github.com/user-attachments/assets/aaf88547-41f9-4d34-beea-5f122bde9c21" />

The following are some additional session hijacking tools:
- **JHijack** [https://sourceforge.net]
- **Ettercap** [https://www.ettercap-project.org]

---
## Web Server Password Hacking 
Web server password hacking involves trying different methods to crack login credentials. Attackers may use techniques like guessing, dictionary attacks, brute-force, and rainbow tables. These methods can take time but are often automated using tools like Hashcat, THC Hydra, and Ncrack to speed up the process and break into web servers.

- **Hashcat** 🔗Source: [https://hashcat.net] <br>
	Hashcat is a cracker compatible with multiple OSs and platforms and can perform multi-hash (MD4, 5; SHA – 224, 256, 384, 512; RIPEMD-160; etc.), multi-device password cracking. The attack modes of this tool are straight, combination, brute force, hybrid dict + mask, and hybrid mask + dict.

<img width="521" height="379" alt="image" src="https://github.com/user-attachments/assets/d6e10ea0-7567-48df-bfda-e602b1b63f30" />

- **THC Hydra** 🔗Source: [https://www.kali.org/tools/hydra/] <br>
	**THC Hydra** is a powerful and fast password-cracking tool used by ethical hackers and security researchers to test how secure login systems are. It can try many username and password combinations (called brute-force attacks) across different services like FTP, SSH, Telnet, HTTP, RDP, MySQL, and more. Hydra supports many protocols and shows how easily attackers can break into systems if weak passwords are used.

<img width="576" height="353" alt="image" src="https://github.com/user-attachments/assets/738da420-598f-4f95-a59a-069676f5be33" />

The following are some additional password cracking tools:
- **Ncrack** [https://nmap.org]
- **Rainbow crack** [https://project-rainbowcrack.com]
- **Wfuzz** [http://www.edge-security.com]
- **Wireshark** [https://www.wireshark.org]

---
## Using Application Server as a Proxy 
Attackers can misuse web servers that are set up as proxies (forwarding or reverse) to hide their identity and launch attacks. They use special HTTP requests like **GET** and **CONNECT** to:

* Attack other systems online
* Access internal network devices
* Connect to services on the same proxy server

If a web server is not properly secured, attackers can use it as a tool to reach their real targets.

<img width="687" height="123" alt="image" src="https://github.com/user-attachments/assets/7bda59e8-0092-4564-b200-47633bb788af" />

---
## Path Traversal via Misconfigured NGINX Alias 
A **Path Traversal via Misconfigured NGINX Alias** happens when the `alias` directive in the `nginx.conf` file is set **without a trailing slash (`/`)**, which can lead to serious security issues.

### Example of Misconfiguration:

```nginx
location /i {
    alias /data/w3/images/;
}
```

In this setup, Nginx may treat URLs like `/i/../secret.txt` as valid paths and allow attackers to **traverse directories** outside the intended `/data/w3/images/` folder. This lets them access sensitive files like system logs or config files.

Attackers use `../` in the URL to **move up the directory tree** and read files that should be protected.

### Exploiting This Vulnerability Using Kyubi 
🔗Source: [https://github.com/apache/kyuubi]

Kyubi is a Python-based security tool designed to identify and exploit path-traversal vulnerabilities in Nginx web servers caused by misconfigured alias directives. Kyubi automates the process of testing URL paths for potential traversal problems. <br>
Run the following command to scan for misconfigurations/vulnerabilities: 
```bash
kyubi -v <target_URL>
```

<img width="633" height="138" alt="image" src="https://github.com/user-attachments/assets/f3acaa71-0d9f-439e-9170-ae14c7b71c94" />

Kyubi recursively brute forces the URL path to detect and list files or directories that are potentially exposed owing to misconfigurations. Exploiting this misconfiguration allows attackers to access sensitive files outside the Webroot, including configuration files and credentials. This can lead to severe consequences such as data theft, website defacement, and even total web server compromise. 

---
## Web Server Attack Tools 
- **Immunity’s CANVAS** 🔗Source: [https://www.immunityinc.com] <br>
	Immunity’s CANVAS provides penetration testers and security professionals with hundreds of exploits, an automated exploitation system, and a comprehensive, reliable exploit development framework. It provides features such as client-side exploitation, privilege escalation, HTTP tunneled privilege escalation, remote kernel exploitation, advanced backdoor technology, and advanced web attack technology.

<img width="586" height="496" alt="image" src="https://github.com/user-attachments/assets/b537eb4c-f73f-4490-a25a-d4618ca5e350" />

The following are some additional web server attack tools:
- **OpenVAS** [https://www.openvas.org]
- **THC Hydra** [https://www.kali.org/tools/hydra/]
- **HULK DoS** [https://github.com/grafov/hulk]
- **MPack** [https://sourceforge.net]
